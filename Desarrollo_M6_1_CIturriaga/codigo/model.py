"""
    model.py: modulo donde se especifica el modelo
"""
from sklearn.linear_model import LinearRegression

RND_STATE=42


def reg_lineal(X, y):
    """Documentación de este modelo: """
    clf = reg_lineal(random_state=RND_STATE)
    return clf.fit(X, y)